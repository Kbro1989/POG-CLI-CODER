# POG-CLI-CODER — POG CLI Coding Tool

> JavaScript · CLI · Node.js

Command-line coding tool for the POG2 development workflow. Used as the local
bridge endpoint (`http://127.0.0.1:3040`) by the `cloudflare-code-agent` COMMANDER AGENT.

```
package.json
```

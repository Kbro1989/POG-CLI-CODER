name = "pog-vibe-worker"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
# account_id = "6872653edcee9c787c1b783173793" # Managed via environment variable

[vars]
# Environment variables will be set via `wrangler secret put` or dashboard

[durable_objects]
bindings = [
  { name = "RATE_LIMITER", class_name = "RateLimiter" }
]

[ai]
binding = "AI"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RateLimiter"]
